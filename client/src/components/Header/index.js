import React from "react";
import NavBar from "./NavBar";

const Header  = () => {
    

    return (
        <header class="hero">
            <NavBar />
        </header>
    );
};

export default Header;